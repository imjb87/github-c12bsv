---
const { blok } = Astro.props
---

<div class="relative group mb-4 w-full">
  <img class="w-full" src={blok.image.filename} alt={blok.image.alt} />
  <div class="absolute top-0 left-0 w-full h-full flex-col items-center justify-center px-8 bg-yellow-500 bg-opacity-95 hidden group-hover:flex">
    <h3 class="text-lg font-extrabold text-gray-700 uppercase mb-3">
      {blok.title}
    </h3>
    <p class="text-white text-base mb-4">{blok.description}</p>
  </div>
</div>